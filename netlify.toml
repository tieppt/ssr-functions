[build]
  command = "npm run build:ssr" # the command you run to build this file
  functions = "functions"
  publish = "dist/browser" # CLI builds to this folder, Netlify should serve all these files statically

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
